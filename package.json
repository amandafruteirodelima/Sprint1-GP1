{
  "name": "Sprint1-GP1",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node ./APIConfigurada(funcionando)/api-projeto-site/API-projeto-site/bin/www",
    "postinstall": "cd APIConfigurada(funcionando) && cd api-projeto-site && cd API-projeto-site && npm install"
  }
}
